# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files — .env.example compris : il contient l'URL du projet réel.
.env
.env.*

# Supabase : état local du conteneur, et types régénérés par `npm run db:types`
# (référence de comparaison — server/database/types.ts reste la source tenue).
supabase/.branches
supabase/.temp
server/database/types.generes.ts
.env.example

# Vidéos : sources et flux transcodés. Volumineux et reproductibles — ils
# vivent sur le stockage d'objets, pas dans l'historique Git.
medias/sources
medias/hls
docs/Comptes-de-test-E-Masterclass.xlsx
docs/email-testeurs.md
